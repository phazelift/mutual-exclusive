// Generated by CoffeeScript 2.7.0
(function () {
  var MessageEvents, allString, message, mutuallyExclusive;
  ({
    allString
  } = require('types.js'));
  MessageEvents = require('message-events');
  message = new MessageEvents();
  message.format('error', function (text) {
    return {
      sender: 'mutually-exclusive',
      type: 'error',
      text: text
    };
  });

  mutuallyExclusive = function (a, b) {
    var ch, i, len;

    if (!allString(a, b)) {
      return message.error('requires string type arguments');
    }

    for (i = 0, len = a.length; i < len; i++) {
      ch = a[i];

      if (b.indexOf(ch) > -1) {
        return false;
      }
    }

    return true;
  };

  mutuallyExclusive.onError = function (handler) {
    return message.on('error', handler);
  };

  module.exports = mutuallyExclusive;
}).call(this);