{
  "name": "@sygn/mutually-exclusive",
  "description": "check whether the characters of two strings are mutually exclusive",
  "version": "0.1.1",
  "main": "mutually-exclusive",
  "publishConfig": {
		"access": "public"
	},
	"dependencies": {
		"message-events": "^0.4.0",
		"types.js": "^1.9.4"
	 },
  "devDependencies": {},
  "scripts": {
	"build": "coffee -c --transpile mutually-exclusive.coffee && npm run uglify",
    "test": "echo \"Error: no test specified\" && exit 1",
	 "uglify": "terser mutually-exclusive.js -c -m -o mutually-exclusive.min.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/phazelift/mutually-exclusive.git"
  },
  "keywords": [
    "mutually",
	 "exclusive",
	 "string",
	 "characters"
  ],
  "author": "dennis raymondo van der sluis",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/phazelift/mutually-exclusive/issues"
  },
  "homepage": "https://github.com/phazelift/mutually-exclusive#readme"
}
